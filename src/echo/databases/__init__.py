"""Database engine clients for Echo SDK.

Engines live as subpackages here (postgres, ...). Each subpackage is
import-guarded — installing the engine driver is opt-in via extras
like `echo[postgres]`.
"""
