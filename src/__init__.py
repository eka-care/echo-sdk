"""
echo - Base Medical Agents for Eka Care

This is the src layout container. Import from 'echo' directly:
    from echo import BaseAgent, LLMConfig, get_llm
"""

# src-layout: users import 'echo' package directly, not 'src'
