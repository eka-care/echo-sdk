# Ev variables to load the default LLM for agents in case if not passed 
ECHO_DEFAULT_LLM_MODEL=anthropic.claude-3-haiku-20240307-v1:0
ECHO_DEFAULT_PROVIDER=bedrock
ECHO_DEFAULT_LLM_TEMPERATURE=0.2
ANTHROPIC_API_KEY='API KEY FOR ANTHROPIC'


# Credentials for using bedrock
AWS_ACCESS_KEY_ID="AWS Access Key Id"
AWS_SECRET_ACCESS_KEY="AWS Secret Access key"
AWS_SESSION_TOKEN="AWS Session Token"
AWS_DEFAULT_REGION=ap-south-1


# Audio transcription — Sarvam (provider="sarvam")
# Used by both SarvamTranscriber (REST) and SarvamRealtimeClient (streaming).
SARVAM_API_KEY="SARVAM API SUBSCRIPTION KEY"

# Optional overrides. SARVAM_BASE_URL retargets the REST client (proxies,
# self-hosted gateways); SARVAM_REALTIME_URL retargets the streaming socket.
# SARVAM_BASE_URL="https://api.sarvam.ai"
# SARVAM_REALTIME_URL="wss://api.sarvam.ai/speech-to-text/ws"
# SARVAM_STT_MODEL="saarika:v2.5"


# Prmopt provider
ECHO_PROMPT_PROVIDER = "langfuse"

# if Lanfuse is the provider for prompt set this
LANGFUSE_SECRET_KEY = "LANGFUSE_SECRET_KEY"
LANGFUSE_PUBLIC_KEY = "LANGFUSE_PUBLIC_KEY"
LANGFUSE_BASE_URL = "https://langfuse.xxx.xxx"